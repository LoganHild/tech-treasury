{{#each posts as |post|}}

<div class="card">
  <header class="card-header card-header-title">
    
    <div class="column">
      <a href="/post/{{id}}"  class="float-right">{{title}}</a></p>
    </div>

    <div class="column">
    </div>
    
  </header>

    <div class="card-content">
        <div class="content has-text-justified">
            {{text}}
        </div>
            <div class="is-pulled-right">
                <b>{{user.username}}</b> | 
                 | {{format_date date_created}} | <a href="post/{{id}}"></a>
            </div>
    </div>

</div>

{{else}}

<h3 class="float-left">Global Posts</h3>

{{/each}}